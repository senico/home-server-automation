[global]
   workgroup = WORKGROUP
   server string = Samba Server
   security = user
   map to guest = bad user
   dns proxy = no

[{{ smb_share_name }}]
   path = {{ mount_point }}
   browsable = yes
   writable = yes
   guest ok = no
   read only = no
   valid users = {{ smb_user }}
